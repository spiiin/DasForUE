//I can't `using namespace das` cause of das/unreal name conflicts (TArray, TTuple, etc?), but still don't want using full names
using das::ManagedStructureAnnotation;
using das::ModuleLibrary;
using das::Module;
using das::FsFileAccess;
using das::SideEffects;
using das::ModuleGroup;
using das::Program;
using das::Context;
using das::make_smart;